import { SectionTitle } from "components/Typography";

const Contact = () => {
  return (
    <section id='contact' className='w-full h-screen snap-start '>
      <SectionTitle subtitle="Let's be in touch"> Contact </SectionTitle>
    </section>
  );
};

export default Contact;
